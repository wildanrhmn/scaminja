{
  "name": "scaminja",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scaminja — an A2MCP Agent Service Provider on OKX.AI. Paste any message, link, email, or wallet address and get an instant, evidence-backed Safe / Caution / Scam verdict.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "build:web": "npm --prefix web install && npm --prefix web run build",
    "build:all": "npm run build:web && npm run build",
    "start": "node dist/index.js",
    "smoke": "tsx test/smoke.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "@okxweb3/x402-core": "^0.1.0",
    "@okxweb3/x402-evm": "^0.2.1",
    "@okxweb3/x402-express": "^0.1.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.10.0",
    "tsx": "^4.23.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
